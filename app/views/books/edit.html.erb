<h2>Editing Book</h2>
<%= form_with model: @book do |f| %>

  <h5>Tilte</h5>
  <%= f.text_field :title %>

  <h5>Opinion</h5>
  <%= f.text_area :body %>

  <%= f.submit 'Update Book'%>

  <p>
    <%= link_to "show", book_path(@book.id) %> /
    <%= link_to "back", boos_path %>
  </p>

<% end %>